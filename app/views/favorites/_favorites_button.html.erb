<% if current_user.favorite?(micropost) %>
  <%= link_to "unlike", micropost_favorites_path(micropost), method: :delete %>
  <%#= form_for(current_user.favorites.find_by(micropost_id: user.id), html: { method: :delete }) do |f| %>
    <%#= hidden_field_tag :micropost_id, micropost.id %>
    <%#= f.submit 'Unlike', class: 'btn btn-danger' %>
  <%# end %>
<% else %>
  <%= link_to "like", micropost_favorites_path(micropost), method: :post %>
  <%#= form_for(current_user.favorites.build, url: micropost_favorites_path(micropost)) do |f| %>
    <%#= hidden_field_tag :micropost_id, micropost.id %>
    <%#= f.submit 'like', class: 'btn btn-primary' %>
  <%# end %>
<% end %>
